#[derive(Default)]
pub struct Dupe{
    pub name: String,
    pub health: f32,
    pub max_health: f32,
    pub build_speed: f32,
    pub calories: f32,
    pub max_calories: f32,
    pub hunger_rate: f32,
}